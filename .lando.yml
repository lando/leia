name: leia
services:
  node:
    type: node:24
    build:
      - npm install --global "$(node -p 'require("./package.json").packageManager')"
      - bun install --frozen-lockfile
tooling:
  node:
    service: node
  bun:
    service: node
