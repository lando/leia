
// We need these deps to run our tezts
const chai = require('{{=it.deps}}/chai');
const CliTest = require('{{=it.deps}}/command-line-test');
const debug = require('{{=it.deps}}/debug')('leia:test:{{=it.id}}');
const path = require('path');
chai.should();

/* eslint-disable max-len */
