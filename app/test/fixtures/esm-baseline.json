[
  "/*",
  " * This file was automatically generated, editing it manually would be foolish",
  " *",
  " * See https://github.com/lando/leia for more",
  " * information on how all this magic works",
  " *",
  " */",
  "",
  "// Set some helpful envvars so we know these are leia tezts",
  "process.env.LEIA = 'true';",
  "process.env.LEIA_ENVIRONMENT = 'true';",
  "process.env.LEIA_TEST_RUNNING = 'true';",
  "process.env.LEIA_VERSION = \"fixture\";",
  "",
  "// Set legacy envars",
  "// These are DERECATED and will eventually be removed!!!",
  "process.env.LEIA_PARSER_RUNNING = 'true';",
  "process.env.LEIA_PARSER_VERSION = \"fixture\";",
  "process.env.LEIA_PARSER_ID = \"fixture\";",
  "process.env.LEIA_PARSER_RETRY = 3;",
  "",
  "import {createRequire} from 'node:module';",
  "",
  "// Leia's dependencies remain CommonJS, so resolve them with Node's cross-platform CommonJS loader.",
  "const require = createRequire(import.meta.url);",
  "const chai = require(\"/deps/chai\");",
  "const CliTest = require(\"/deps/clt\");",
  "const debug = require(\"/deps/debug\")('leia:test:' + \"fixture\");",
  "const path = require('path');",
  "chai.should();",
  "",
  "/* eslint-disable max-len */",
  "describe(\"fixture\", function() {",
  "  this.retries(3);",
  "",
  "  // These are tests we need to run to get the app into a state to test",
  "  // @todo: It would be nice to eventually get these into mocha before hooks",
  "  // so they run before every test",
  "  it(\"should preserve bytes\", done => {",
  "    process.chdir(\"/fixture\");",
  "    process.env.LEIA_TEST_ID = \"fixture\";",
  "    process.env.LEIA_TEST_NUMBER = 1;",
  "    process.env.LEIA_TEST_RETRY = this.ctx.test._currentRetry;",
  "    process.env.LEIA_TEST_STAGE = \"setup\";",
  "    ",
  "    const cli = new CliTest();",
  "    const data = {shell: \"sh\", args: [\"/scripts/test.sh\"], commands: \"printf '%s\\\\n' \\\"${HOME}\\\" `literal` \\\\1\\nnext\", stdin: \"pipe\"};",
  "    debug('running test %s from %s using %o', \"fixture\", \"/fixture\", data);",
  "    cli.spawn(\"sh\", [\"/scripts/test.sh\"], {stdio: [\"pipe\", 'pipe', 'pipe']}).then(res => {",
  "      if (res.error === null) {",
  "        done();",
  "      } else {",
  "        const error = [`CODE: ${res.error.code}`, `STDOUT: ${res.stdout}`, `STDERR: ${res.stderr}`].join('\\n');",
  "        done(new Error(error));",
  "      }",
  "    });",
  "  });",
  "",
  "  // These tests are the main event",
  "  // @todo: It would be nice to eventually get these into mocha after hooks",
  "  // so they run after every test",
  "  it(\"should preserve bytes\", done => {",
  "    process.chdir(\"/fixture\");",
  "    process.env.LEIA_TEST_ID = \"fixture\";",
  "    process.env.LEIA_TEST_NUMBER = 1;",
  "    process.env.LEIA_TEST_RETRY = this.ctx.test._currentRetry;",
  "    process.env.LEIA_TEST_STAGE = \"test\";",
  "    ",
  "    const cli = new CliTest();",
  "    const data = {shell: \"sh\", args: [\"/scripts/test.sh\"], commands: \"printf '%s\\\\n' \\\"${HOME}\\\" `literal` \\\\1\\nnext\", stdin: \"pipe\"};",
  "    debug('running test %s from %s using %o', \"fixture\", \"/fixture\", data);",
  "    cli.spawn(\"sh\", [\"/scripts/test.sh\"], {stdio: [\"pipe\", 'pipe', 'pipe']}).then(res => {",
  "      if (res.error === null) {",
  "        done();",
  "      } else {",
  "        const error = [`CODE: ${res.error.code}`, `STDOUT: ${res.stdout}`, `STDERR: ${res.stderr}`].join('\\n');",
  "        done(new Error(error));",
  "      }",
  "    });",
  "  });",
  "",
  "  it(\"should preserve bytes\", done => {",
  "    process.chdir(\"/fixture\");",
  "    process.env.LEIA_TEST_ID = \"fixture\";",
  "    process.env.LEIA_TEST_NUMBER = 2;",
  "    process.env.LEIA_TEST_RETRY = this.ctx.test._currentRetry;",
  "    process.env.LEIA_TEST_STAGE = \"test\";",
  "    this.ctx.test.skip();",
  "  });",
  "",
  "  // These are tests we need to run to get the app into a state to test",
  "  // @todo: It would be nice to eventually get these into mocha before hooks",
  "  // so they run before every test",
  "  it(\"should preserve bytes\", done => {",
  "    process.chdir(\"/fixture\");",
  "    process.env.LEIA_TEST_ID = \"fixture\";",
  "    process.env.LEIA_TEST_NUMBER = 1;",
  "    process.env.LEIA_TEST_RETRY = this.ctx.test._currentRetry;",
  "    process.env.LEIA_TEST_STAGE = \"cleanup\";",
  "    ",
  "    const cli = new CliTest();",
  "    const data = {shell: \"sh\", args: [\"/scripts/test.sh\"], commands: \"printf '%s\\\\n' \\\"${HOME}\\\" `literal` \\\\1\\nnext\", stdin: \"pipe\"};",
  "    debug('running test %s from %s using %o', \"fixture\", \"/fixture\", data);",
  "    cli.spawn(\"sh\", [\"/scripts/test.sh\"], {stdio: [\"pipe\", 'pipe', 'pipe']}).then(res => {",
  "      if (res.error === null) {",
  "        done();",
  "      } else {",
  "        const error = [`CODE: ${res.error.code}`, `STDOUT: ${res.stdout}`, `STDERR: ${res.stderr}`].join('\\n');",
  "        done(new Error(error));",
  "      }",
  "    });",
  "  });",
  "});",
  "",
  ""
]
