name: Run Module Format Tests

on:
  pull_request:

permissions:
  contents: read

jobs:
  module-format-tests:
    name: module-format-tests (${{ matrix.format }}, ${{ matrix.runtime }})
    runs-on: ubuntu-24.04
    env:
      TERM: xterm
      LEIA_RUNTIME: ${{ matrix.runtime }}
    strategy:
      fail-fast: false
      matrix:
        format:
          - commonjs
          - esm
        runtime: [source, built]
        include:
          - runtime: source
            cli: bun bin/leia.ts
          - runtime: built
            cli: node dist/bin/leia.js

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      - name: Install Node from .node-version
        uses: actions/setup-node@v7
        with:
          node-version-file: '.node-version'
      - name: Install Bun from .bun-version
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version
      - name: Check Bun version
        run: bun run check:toolchain
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Build Node entrypoints
        run: bun run build

      - name: Test module format CLI contract
        shell: bash
        run: |
          ${{ matrix.cli }} --help | grep -- '--module-format=auto|commonjs|esm'
          if ${{ matrix.cli }} examples/basic-example.md --module-format amd; then
            echo 'Leia accepted an unsupported module format'
            exit 1
          fi

      - name: Run Leia examples
        shell: bash
        env:
          MODULE_FORMAT: ${{ matrix.format }}
        run: |
          ${{ matrix.cli }} examples/basic-example.md --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} "examples/**/*.md" -i "examples/basic-cmd-example.md" -i "examples/exclude-example/**/*.md" -i "examples/stdin-example.md" -i "examples/custom-headers.md" -i "examples/environment.md" -i "examples/basic-pwsh-example.md" --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} examples/custom-headers.md -s Hello -t Sup -c Goodbye --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} examples/basic-example.md --retry 2 --split-file --stdin --spawn --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} examples/custom-headers.md -s Blue,No,Yellow,Hello -t Sup -c Goodbye --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} examples/environment.md examples/basic-example.md --retry 4 --timeout 5 --module-format "$MODULE_FORMAT"
          ${{ matrix.cli }} examples/basic-example.md --module-format "$MODULE_FORMAT" --shell bash
          ${{ matrix.cli }} examples/basic-example.md --module-format "$MODULE_FORMAT" --shell sh

      - name: Test explicit package-scope overrides
        shell: bash
        env:
          MODULE_FORMAT: ${{ matrix.format }}
        run: bash examples/format-examples/run.sh "$MODULE_FORMAT"
